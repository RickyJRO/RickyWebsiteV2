export interface IProject {
    projectName: string;
    projectDescription: string;
    responsabilityDescription: string;
    stacks: [{ name: string}];
    projectLink: string;
    image:string;
  }